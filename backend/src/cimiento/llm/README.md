# LLM — Capa 1

Orquestación conversacional con LangGraph + Ollama.

- `agents/`: agentes individuales (requisitos, normativo, validador, etc.)
- `graphs/`: grafos que conectan agentes con ciclos
- `tools/`: herramientas que los agentes pueden invocar (solver, RAG, etc.)
